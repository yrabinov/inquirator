<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta content="IE=edge" http-equiv="X-UA-Compatible" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Inquirator</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= stylesheet_link_tag bootstrap_css, bootstrap_theme_css, :application %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag bootstrap_js, jquery_js, jquery_ui_js %>
  <%= csrf_meta_tags %>
</head>
<body>
  <%= navbar inverted: true, position: :top do %>
    <%= horizontal do %>
      <%= nav do %>
        <%= link_to 'Questions', question_queues_path %>
        <%= link_to 'My Team', team_members_path %>
        <%= link_to 'Managers', users_path %>
      <% end %>
      <%= button_to 'Sign out', destroy_user_session_path, form_class: 'navbar-right', class: 'btn-link', method: :delete %>
    <% end %>
  <% end if user_signed_in? %>
  <div class='container'>
    <%- flash.map do |priority, message| %>
      <%= alert_box message, priority: priority %>
    <% end %>
    <%= content_for?(:container) ? yield(:container) : yield %>
  </div>
</body>
</html>
